// Tailwind v4 — configuration is CSS-first via globals.css (@import "tailwindcss", @theme, @custom-variant).
// This file is intentionally minimal. Do NOT add v3-style options (darkMode, theme.extend, plugins, prefix)
// as they will cause errors in v4.
//
// Content paths are auto-discovered by Tailwind v4 from the filesystem.
// All design tokens (colors, radius, keyframes, dark mode) are defined in src/app/globals.css.

export default {};
